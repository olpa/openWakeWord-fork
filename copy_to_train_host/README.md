# OpenWakeWord Training Deployment

Ansible playbook to setup OpenWakeWord training environment on a remote host.

## Usage

```bash
ansible-playbook -i <host>, 10_setup_project.yml
```

Replace `<host>` with your remote training host (e.g., `user@192.168.1.100`).
